repos:
  - repo: local
    hooks:
      - id: sync-docs-metadata
        name: Sync Documentation Metadata
        entry: python scripts/sync_docs_metadata.py
        language: python
        files: ^src/.*\.py$
        pass_filenames: false
        always_run: true

      - id: sync-quartz-config
        name: Sync Quartz Configuration
        entry: python scripts/sync_quartz_configs.py
        language: python
        files: ^quartz-config/.*
        pass_filenames: false
        always_run: true
