# ONNX Model Filename (must exist in models/ directory)
ONNX_CHECKPOINT_FILENAME=last-checkpoint-words_502.pth.onnx

# Domain Name for CORS (e.g., http://localhost:8000 or *)
DOMAIN_NAME=http://localhost:8000

# Set to 1 for local/docker development (uses ./data instead of /kaggle)
LOCAL_DEV=1
USE_CPU=1
